---
type Props = {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
};

const { class: className = "", as: Tag = "div" } = Astro.props;
---

<Tag class={`mx-auto w-full max-w-6xl px-6 ${className}`}>
  <slot />
</Tag>

